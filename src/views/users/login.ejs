<!DOCTYPE html>
<html lang="en">

<head>
    <title>Login</title>
    <%- include("./partials/head") %>
        <link rel="stylesheet" href="/css/register-login.css">
</head>

<body>
    <%- include("./partials/header") %>

        <%- include("./partials/menuMobile") %>
            <main>
                <div class="img-form">
                    <img src="images/contenido-pagina/sneakers-fondo-form.png" alt="fondo formulario">
                </div>
                <div class="container">
                    <div id="jumpstore-form">
                        <form class="formulario" id="formulario" action="/login" method="POST">
                            <div class="txt-title-content">
                                <h1>Entrar</h2>
                                    <h2>¡Bienvenido de nuevo! Inicié sesión en su cuenta de Jumpstore.</h2>
                            </div>

                            <div class="field email" id="grupo__email">
                                <label for="Email" class="formulario__label">Correo Electronico:</label>
                                <div class="formulario__grupo-input">
                                    <input type="email" id="email" name="email"  class="formulario__input">
                                    <i class="formulario__validacion-estado fas fa-times-circle"></i>
                                </div>

                                <% if (locals.errors && errors.email) { %>
                                    <div class="text-danger">
                                        <%= errors.email.msg %>
                                    </div>
                                    <% } %>
                                        <p class="formulario__input-error">El usuario tiene que ser de 4 a 16 dígitos y
                                            solo puede contener numeros, letras y guion bajo.</p>

                            </div>
                            <div class="field psw" id="grupo__password">
                                <label for="password" class="formulario__label">Contraseña:</label>
                                <div class="formulario__grupo-input">
                                    <input type="password" name="password" id="password"  class="formulario__input"> <i
                                        class="fas fa-eye eyePsw"></i>
                                <% if (locals.errors && errors.password) { %>
                                    <div class="text-danger">
                                        <%= errors.password.msg %>
                                    </div>
                                    <% } %>
                                </div>
                                <p class="formulario__input-error">La contraseña tiene que ser de 4 a 12 dígitos.</p>
                            </div>

                            <div class="field terminos">
                                <input type="checkbox" name="recordame" id="recordame">
                                <label for="recordame">Recordar mi usuario</label>
                            </div>
                            <div class="field-cancelar-registrar">
                                <button type="reset" id="delete-button">Cancelar</button>
                                <button type="submit">
                                    Iniciar sesión
                                </button>
                            </div>
                            <div class="miembro">
                                ¿No tienes cuenta?
                                <a href="/register">Registrarse</a>
                            </div>

                        </form>
                    </div>
                </div>
            </main>
            <%- include("./partials/footer") %>
                <%- include("./partials/scripts") %>
                    <script src="/js/register-login.js"></script>
                    <script src="/js/UserRegister.js"></script>

</body>

</html>