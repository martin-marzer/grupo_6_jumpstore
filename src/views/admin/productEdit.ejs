<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Editar producto</title>
        <%- include("./partials/head") %>
        <link rel='stylesheet' href='/css/productEdit.css'>
    </head>
<body>
    <%- include("./partials/header") %>
    <%- include("./partials/menuMobile") %>

    <main class="container_main">
        <form class="formulario-edit" method="POST" action="/administratorToolsProducts?_method=PUT">
            <h1 class="titulo">Editando <%= articulo.name %></h1>
            <div>
                <input class="formulario-input" type="text" name="nombre" class="cambiar-producto" placeholder="<%= articulo.name %>">
            </div>
            <div>
                <input class="formulario-input" type="text" name="talle" class="crear-producto" placeholder="<%= articulo.talle %>" required>
            </div>
            <div class="col-12">
                <label for="description" class="form-label">Descripción:</label>
                <textarea name="descripcion" id="description" class="descripcion" required></textarea>
            </div>
            <div>
                <input class="formulario-input" type="number" name="precio" class="cambiar-producto" placeholder="Nuevo precio del producto">
            </div>
            <div class="marcas">
                <select id="select-marcas">
                    <option value="seleccionar marca">seleccionar marca</option>
                    <option value="Nike">Nike</option>
                    <option value="Addidas">Addidas</option>
                    <option value="Jordan">Jordan</option>
                    <option value="Fila">Fila</option>
                    <option value="Vans">Vans</option>
                </select>
            </div>
            <div class="file-upload">
                <input class="file-upload__input" type="file" name="myFile[]" id="myFile" multiple>
                <button class="file-upload__button" type="button">Nueva imagen</button>
                <span class="file-upload__label"></span>
              </div>
            <section class="wrapper">
                <button type="submit" class="add_edit_product"><a href="/administratorToolsProducts" class="link-boton">Guardar</a></button>
                <button type="reset" class="add_edit_product">Borrar</button>
                </section>
            </form>
    </main>
    <%- include("./partials/scripts") %>
    <script src="/js/edit-CreateProduct.js"></script>
</body>
</html>